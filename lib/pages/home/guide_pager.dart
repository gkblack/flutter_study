import 'package:flutter/material.dart';

//导航功能

class GuidePager extends StatefulWidget{

  @override
  _GuidePagerState createState() => _GuidePagerState();

}

class _GuidePagerState extends State<GuidePager>{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('guide'),
      ),
      body: new Center(
        child: new Column(

        ),
      ),
    );
  }

}